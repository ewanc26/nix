# PLACEHOLDER — replace with: sops --encrypt --age <key> pds.env > secrets/pds.env
# Required keys:
#   PDS_JWT_SECRET=<openssl rand --hex 16>
#   PDS_ADMIN_PASSWORD=<openssl rand --hex 16>
#   PDS_PLC_ROTATION_KEY_K256_PRIVATE_KEY_HEX=<see atproto PDS docs>
#   PDS_EMAIL_SMTP_URL=<optional>
#   PDS_EMAIL_FROM_ADDRESS=<optional>
